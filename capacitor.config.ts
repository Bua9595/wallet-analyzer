import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.deinname.walletanalyzer',
  appName: 'WalletAnalyzer',
  webDir: 'dist'
};

export default config;
